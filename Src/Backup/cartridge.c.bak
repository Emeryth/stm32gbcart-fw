/*
 * cartridge.c
 *
 *  Created on: Jan 2, 2020
 *      Author: andrju
 */

#include "cartridge.h"
#include <string.h>
//#include "gejmboj.h"
#include "lsdj.h"
#include "save.h"

uint8_t sram[RAM1_SIZE];
uint8_t sram2[RAM2_SIZE]__attribute__((section(".ccmram")));

cartridge_t cartridge={
		.rom=lsdj_gb,
		.ram=sram,
		.rom_bank=1,
		.ram_bank=0,
};

void load_save(void){

	memcpy(sram,red_yellow_orange_sav,RAM1_SIZE);
	memcpy(sram2,red_yellow_orange_sav+RAM1_SIZE,RAM2_SIZE);

}
